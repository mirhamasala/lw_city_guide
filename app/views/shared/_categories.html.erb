<div class="categories spacer-md">
  <%= link_to "all", city_spots_path(@city), class: "categories__all" %>
  <% Category.all.each do |category| %>
    <%= link_to category.name, city_spots_path(category: category.name), class: "categories__name" %>
  <% end %>
</div>
