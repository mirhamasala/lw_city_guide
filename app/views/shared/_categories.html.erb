<div class="categories">
  <%= link_to "all", city_spots_path(@city) %>
  <% @categories.each do |category| %>
    <%= link_to category.name, city_spots_path(:category => category) %>
  <% end %>
</div>
